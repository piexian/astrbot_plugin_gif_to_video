{
  "enabled_provider_id": {
    "description": "指定启用 GIF 转换的服务商",
    "type": "string",
    "hint": "留空以自动适配您的默认模型。若要为某个特定模型启用，请在此处选择它的服务商ID。",
    "default": "",
    "_special": "select_provider"
  },
  "preview_frame_count": {
    "type": "int",
    "title": "GIF 预览帧数",
    "description": "转换为视频后，再提取多少帧 PNG 附带给 LLM。数值越大越接近原动图，但也会增加请求体积。",
    "default": 4
  }
}
